pre-commit:
  parallel: false
  commands:
    pretty:
      glob: "*.{js,ts,jsx,tsx,json}"
      run: yarn pretty
    lint:
      glob: "*.{js,ts,jsx,tsx}"
      run: yarn lint
      stage_fixed: true
